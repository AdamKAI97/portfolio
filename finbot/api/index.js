/**
 * Точка входа для облака (Vercel).
 * Один обработчик обслуживает всё: вебхук Telegram, напоминания и API.
 *
 * Все адреса вида /api/* приводятся к этому файлу правилом из vercel.json,
 * а настоящий путь передаётся параметром __path — так маршрутизация
 * не зависит от того, как хостинг обрабатывает вложенные адреса.
 */
import { createApp } from '../backend/src/app.js';

const app = createApp();

export default app;
